import { SearchMutationDirective } from './search-mutation.directive';

describe('SearchMutationDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchMutationDirective();
    expect(directive).toBeTruthy();
  });
});

import { FollowIntersectionDirective } from './follow-intersection.directive';

describe('FollowIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FollowIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});

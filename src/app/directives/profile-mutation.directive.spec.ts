import { ProfileMutationDirective } from './profile-mutation.directive';

describe('ProfileMutationDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileMutationDirective();
    expect(directive).toBeTruthy();
  });
});

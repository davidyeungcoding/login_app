import { FollowingMutationDirective } from './following-mutation.directive';

describe('FollowingMutationDirective', () => {
  it('should create an instance', () => {
    const directive = new FollowingMutationDirective();
    expect(directive).toBeTruthy();
  });
});

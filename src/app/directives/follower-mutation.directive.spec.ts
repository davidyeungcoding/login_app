import { FollowerMutationDirective } from './follower-mutation.directive';

describe('FollowerMutationDirective', () => {
  it('should create an instance', () => {
    const directive = new FollowerMutationDirective();
    expect(directive).toBeTruthy();
  });
});

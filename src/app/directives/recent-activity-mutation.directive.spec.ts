import { RecentActivityMutationDirective } from './recent-activity-mutation.directive';

describe('RecentActivityMutationDirective', () => {
  it('should create an instance', () => {
    const directive = new RecentActivityMutationDirective();
    expect(directive).toBeTruthy();
  });
});
